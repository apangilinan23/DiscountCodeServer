namespace Common
{
    /// <summary>
    /// Will be the basis of the server handler to execute
    /// </summary>
    public enum ClientRequestType
    {
        GENERATE = 0,
        USE = 1
    }
}
